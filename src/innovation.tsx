import { ArrowRight, CircleDollarSign, MousePointerClick, ShoppingBag, Users } from 'lucide-react'
import type { Lang } from './types'
const compact=(value:number,lang:Lang)=>new Intl.NumberFormat(lang==='pt'?'pt-BR':'en-US',{notation:'compact',maximumFractionDigits:1}).format(value)
export function GrowthDriverTree({values,lang}:{values:Record<string,number>;lang:Lang}){const nodes=[{label:lang==='pt'?'Usuários ativos':'Active users',value:values.users,icon:Users},{label:lang==='pt'?'Sessões':'Sessions',value:values.sessions,icon:MousePointerClick},{label:lang==='pt'?'Compras':'Purchases',value:values.orders,icon:ShoppingBag},{label:lang==='pt'?'Receita rastreada':'Tracked revenue',value:values.revenue,icon:CircleDollarSign,money:true}];return <section className="driver-tree" aria-label={lang==='pt'?'Árvore de direcionadores de crescimento':'Growth driver tree'}><div className="driver-copy"><span>{lang==='pt'?'INOVAÇÃO · ÁRVORE DE DRIVERS':'INNOVATION · DRIVER TREE'}</span><h2>{lang==='pt'?'Da atenção ao valor':'From attention to value'}</h2><p>{lang==='pt'?'Localize onde o crescimento perde força.':'Locate where growth loses momentum.'}</p></div><div className="driver-nodes">{nodes.map(({label,value,icon:Icon,money},index)=><div className="driver-step" key={label}><article><Icon size={18}/><small>{label}</small><strong>{money?'$':''}{compact(value??0,lang)}</strong></article>{index<nodes.length-1&&<ArrowRight className="driver-arrow" size={20}/>}</div>)}</div></section>}
